<!DOCTYPE chapter>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink"
    version="5.1" xml:id="config" xml:lang="ru">
    <info>
        <title>Описание параметров конфигурации</title>
    </info>

    <para>Для настройки сервера Firebird используется файл <filename>firebird.conf</filename>.
        Настройки считываются из файла один раз при старте сервера, если архитектура SuperSever или
        SuperClassic, и при каждом соединении с базой, если архитектура сервера Classic.</para>
    <para>По умолчанию все параметры в файле конфигурации закомментированы. Для обозначения
        комментариев используется символ <symbol>#</symbol>. Текст, следующий после символа
            <symbol>#</symbol>, до конца строки является комментарием, например:
        <programlisting>
#комментарий
DefaultDbCachePages = 2048 #комментарий    
    </programlisting>
    </para>
    <para>Максимальная длина строки в файле конфигурации сервера равна 80 символов.</para>
    <para>Первое слово в строке, начинающейся не с символа комментария, считается названием
        параметра. Справа от имени параметра, после символа <symbol>=</symbol>, указывается значение
        параметра.</para>
    <section xml:id="config-param-types">
        <title>Типы параметров</title>
        <para>В файле конфигурации присутствуют параметры трех типов: <itemizedlist>
                <listitem>
                    <para>целочисленный (Integer);</para>
                </listitem>
                <listitem>
                    <para>строковый (String);</para>
                </listitem>
                <listitem>
                    <para>логический (Boolean).</para>
                </listitem>
            </itemizedlist></para>

        <simplesect xml:id="config-param-types-integer">
            <title>Целочисленные параметры</title>

            <para>Целочисленные как ясно из названия могут содержать только целые числа. Они могут
                быть записаны со множителями k, m и g.</para>
            <example>
                <title>Целочисленные параметры</title>
                <para>
                    <programlisting>
MaxUnflushedWrites = 100                        
DefaultDbCachePages = 8K        # 8 * 1024 = 8192
DatabaseGrowthIncrement = 128M  # 128 * 1024 * 1024
                    </programlisting>
                </para>
            </example>

            <note>
                <para> Значения параметров, определяющих объем памяти, указываются в байтах. В конце
                    таких значений можно ставить сокращения k, m и g, соответствующие килобайтам,
                    мегабайтам и гигабайтам. </para>
            </note>
        </simplesect>

        <simplesect xml:id="config-param-types-boolean">
            <title>Логические параметры</title>

            <para>Логическое значение выражается в виде целочисленных значений, где 0 (ноль)
                означает <constant>false</constant>, а ненулевое значение означает
                    <constant>true</constant>. Для согласованности мы рекомендуем использовать
                только 0/1. Также строки 'y', 'yes' и 'true' означают <constant>true</constant>, а
                'n', 'no' и 'false' — <constant>false</constant>.</para>
            <example>
                <title>Логические параметры</title>
                <para>
                    <programlisting>
RemoteAccess = true       # true
RemoteFileOpenAbility = 0 # false
                    </programlisting>
                </para>
            </example>
        </simplesect>

        <simplesect xml:id="config-param-types-string">
            <title>Строковые параметры</title>

            <para>Строковые как ясно из названия представляют собой строки.</para>
            <example>
                <title>Строковые параметры</title>
                <para>
                    <programlisting>
RemoteServiceName = gds_db
RemotePipeName = pipe47
                    </programlisting>
                </para>
            </example>
        </simplesect>
    </section>

    <section xml:id="config-scope">
        <title>Область действия</title>

        <para>Параметры конфигурации могут быть применены к экземпляру сервера (Per-server),
            отдельной базе данных (Per-database) или отдельному соединению (Per-connection).</para>
        <para>Параметры, которые могут быть применены к отдельной базе данных можно указывать в
            файле <filename>databases.conf</filename>. В этом случае они перекроют значение
            одноимённого параметра указанного в <filename>firebird.conf</filename>.</para>
        <para>Параметры, которые могут быть применены к отдельному соединению, предназначены для
            использования на стороне клиента. Они устанавливаются с помощью
                <parameter>isc_dpb_config</parameter> параметра в DPB (Database parameters buffer)
                (<parameter>isc_spb_config</parameter> для служб).</para>

        <note>
            <para>Параметры уровня базы данных могут быть установлены используя DPB при первом
                соединении с базой данных, если используется встроенный режим сервера.</para>
        </note>

        <para>Параметры, которые могут быть применены к отдельному соединению, могут быть прочитаны
            из файлов <filename>firebird.conf</filename> и <filename>databases.conf</filename>
            расположенных в том же каталоге, что и клиентская библиотека fbclient. Эти параметры
            будут перекрыты установками через DPB.</para>
    </section>

    <section xml:id="config-macro">
        <title>Макроподстановки</title>

        <para>Существует ряд предопределенных макроподстановок, которые могут быть использованы в
            файлах конфигурации, где требуется имя каталога. Они используют следующий синтаксис:
            <programlisting>
$(<replaceable>&lt;name&gt;</replaceable>)        
        </programlisting>
        </para>
        <para>Полный список таких макроподстановок представлен ниже: <itemizedlist>
                <listitem>
                    <para>root — корневой каталог экземпляра сервера;</para>
                </listitem>
                <listitem>
                    <para>install — директория в которую установлен Firebird. Изначально $(root) и
                        $(install) одинаковые. $(root) может быть переопределена установкой или
                        изменением переменной окружения FIREBIRD. В таком случае эта подстановка
                        отлична от $(install);</para>
                </listitem>
                <listitem>
                    <para>this — каталог, в котором находится текущий файл конфигурации;</para>
                </listitem>
                <listitem>
                    <para>dir_conf — каталог, в котором расположены файлы конфигурации
                            <filename>firebird.conf</filename> и
                        <filename>databases.conf</filename>;</para>
                </listitem>
                <listitem>
                    <para>dir_secDb — директория, в которой расположена база данных безопасности по
                        умолчанию;</para>
                </listitem>
                <listitem>
                    <para>dir_plugins — директория, в которой расположены плагины;</para>
                </listitem>
                <listitem>
                    <para>dir_udf — директория, предназначенная для размещения UDF функций
                        по-умолчанию;</para>
                </listitem>
                <listitem>
                    <para>dir_sample — каталог с примерами (<filename>examples</filename>);</para>
                </listitem>
                <listitem>
                    <para>dir_sampleDb — директория, где лежит пример базы данных
                            (<filename>examples/empbuild</filename>);</para>
                </listitem>
                <listitem>
                    <para>dir_intl — директория, в которой расположены библиотеки
                        интернационализации;</para>
                </listitem>
                <listitem>
                    <para>dir_msg — каталог, в котором расположен файл с сообщениями сервера
                            <filename>firebird.msg</filename>. Обычно он совпадает с $(root), но
                        может быть переопределён переменной окружения FIREBIRD_MSG.</para>
                </listitem>
            </itemizedlist></para>

        <note>
            <para>Внутренние макроподстановки не чувствительны к регистру. Большие буквы
                используются исключительно для повышения читабильности.</para>
        </note>

        <example>
            <title>Пример использования макроподстановки в файле databases.conf</title>

            <para>
                <programlisting>
  employee = $(dir_sampleDb)/employee.fdb          
            </programlisting>
            </para>
        </example>
    </section>

    <section xml:id="config-includes">
        <title>Включение других файлов</title>

        <para>Один файл конфигурации может включать другой с помощью директивы include:
            <programlisting>
include some_file.conf          
            </programlisting>
        </para>

        <para>Относительный путь представляет собой путь по отношению к текущему файлу конфигурации.
            Так, в примере выше файл <filename>/opt/config/master.conf</filename> ссылается на файл
            расположенный по пути <filename>/opt/config/some_file.conf</filename>.</para>

        <para>Директива include поддерживает групповые символы <code>*</code> и <code>?</code>. Все
            совпадающие с шаблоном файлы будут подключены, порядок включения не определён.</para>

        <example>
            <title>Использование групповых символов с директивой include</title>

            <para>
                <programlisting>
include $(dir_plugins)/config/*.conf          
            </programlisting>
            </para>
        </example>
    </section>

    <section xml:id="config-perdb">
        <title>Параметры уровня базы данных</title>

        <para>Настройка конфигурации на уровне базы данных осуществляется с помощью формальных
            записей в файле <filename>databases.conf</filename>. Такие параметры помечены как
            Per-database.</para>

        <section xml:id="config-perdb-format">
            <title>Формат конфигурационных записей</title>

            <para>Если вы не добавляете каких либо специфичных для базы данных директив конфигурации
                для псевдонима, то формат будет такой же, как он был в
                    <filename>aliases.conf</filename> (Firebird 2.5 и ранее):
                <programlisting>
emp = c:\Program Files\examples\empbuild\employee.fdb
# или
emp = /opt/firebird/examples/empbuild/employee.fdb
# или
emp = $(dir_sampleDb)/employee.fdb
            </programlisting>
            </para>
            <para>Несколько более сложный формат используется для случаев, когда определенные не
                глобальные параметры должны быть нацелены на отдельные базы данных. Запись для базы
                данных определяется объявлением псевдонима, как и ранее. Директивы, относящиеся к
                базе данных, перечислены ниже в фигурных скобках.
                <programlisting>
# Directives for MYBIGDB
MYBIGDB = opt/databases/mybigdb.fdb
{
    LockMemSize = 32M           # We know that MYBIGDB needs a lot of locks
    LockHashSlots = 19927       # and a hash table large enough for them
}
            </programlisting>
            </para>
        </section>

        <section xml:id="config-perdb-params">
            <title>Доступные параметры</title>
            <para>Следующие параметры можно скопировать/вставить в файл
                    <filename>database.conf</filename> и использовать в качестве переопределений для
                конкретных баз данных. <table frame="all">
                    <title>Параметры доступные в databases.conf</title>
                    <tgroup cols="3">
                        <colspec colname="c1" colnum="1" colwidth="1.0*"/>
                        <colspec colname="c2" colnum="2" colwidth="1.0*"/>
                        <colspec colname="c3" colnum="3" colwidth="1.0*"/>
                        <tbody>
                            <row>
                                <entry namest="c1" nameend="c3"><emphasis role="bold">Связанные с
                                        ядром сервера</emphasis></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-DatabaseGrowthIncrement"
                                        >DatabaseGrowthIncrement</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-DeadlockTimeout"
                                        >DeadlockTimeout</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-DefaultDbCachePages"
                                        >DefaultDbCachePages</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-EventMemSize"
                                    >EventMemSize</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-FileSystemCacheThreshold"
                                        >FileSystemCacheThreshold</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-ExternalFileAccess"
                                        >ExternalFileAccess</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-GCPolicy">GCPolicy</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-LockAcquireSpins"
                                        >LockAcquireSpins</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-LockHashSlots"
                                    >LockHashSlots</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-LockMemSize"
                                    >LockMemSize</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-MaxUnflushedWrites"
                                        >MaxUnflushedWrites</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-MaxUnflushedWriteTime"
                                        >MaxUnflushedWriteTime</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-SecurityDatabase"
                                        >SecurityDatabase</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-UserManager"
                                    >UserManager</link></entry>
                                <entry/>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-WireCompression"
                                        >WireCompression</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-WireCrypt">WireCrypt</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-WireCryptPlugin"
                                        >WireCryptPlugin</link></entry>
                            </row>
                            <row>
                                <entry><emphasis role="bold">Связанные с клиентом</emphasis></entry>
                                <entry namest="c2" nameend="c3">Некоторые параметры могут быть
                                    настроены в клиентском соединении через DPB/SPB, в качестве
                                    альтернативы их настройке в <filename>database.conf</filename>.
                                    Подробнее смотри в <link linkend="config-scope">Область
                                        действия</link>.</entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-Auth">AuthClient</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-Providers">Providers</link></entry>
                                <entry/>
                            </row>
                            <row>
                                <entry namest="c1" nameend="c3">Следующие параметры могут быть
                                    настроены на стороне клиента только через DPB/SPB.</entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-ConnectionTimeout"
                                        >ConnectionTimeout</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-DummyPacketInterval"
                                        >DummyPacketInterval</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-IpcName">IpcName</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-RemoteAuxPort"
                                    >RemoteAuxPort</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-RemotePipeName"
                                    >RemotePipeName</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-RemoteServicePort"
                                        >RemoteServiceName</link></entry>
                            </row>
                            <row>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-RemoteServicePort"
                                        >RemoteServicePort</link></entry>
                                <entry><link xmlns:xlink="http://www.w3.org/1999/xlink"
                                        linkend="config-param-TcpNoNagle">TCPNoNagle</link></entry>
                                <entry/>
                            </row>
                        </tbody>
                    </tgroup>
                </table></para>
        </section>
    </section>

    <section xml:id="config-common">
        <title>Общие настройки</title>

        <section xml:id="config-param-DatabaseAccess">
            <title>DatabaseAccess</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>DatabaseAccess</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String (специальный формат).</para>
            </formalpara>

            <formalpara>
                <title>Синтаксис:</title>

                <para>
                    <programlisting>
DatabaseAccess = None | Full | { Restrict &lt;<replaceable>dir_1</replaceable>&gt;[;&lt;<replaceable>dir_2</replaceable>&gt;[...;&lt;<replaceable>dir_N</replaceable>&gt;] }
                     </programlisting>
                </para>
            </formalpara>

            <para>Параметр <parameter>DatabaseAccess</parameter> позволяет обеспечить управление
                безопасностью при доступе к файлам базы данных. Доступ к файлам базы данных на
                сервере может быть полным (<literal>Full</literal>), ограниченным
                    (<literal>Restrict</literal>) или запрещённым (<literal>None</literal>).</para>

            <para>Параметр <parameter>DatabaseAccess</parameter> имеет строковый тип; по умолчанию
                значение параметра равно <literal>Full</literal> — полный доступ. Для ограничения
                доступа используется параметр <literal>Restrict</literal>. В этом случае после слова
                    <literal>Restrict</literal> указываются директории, в которых могут быть
                сохранены файлы базы данных.</para>

            <para>При указании списка каталогов могут быть использованы как абсолютные, так и
                относительные пути. Относительные пути считаются от корневого каталога сервера
                Firebird. В качестве разделителя каталогов используется символ
                <quote>;</quote>.</para>

            <para>Если параметр <parameter>DatabaseAccess</parameter> установлен в значение
                    <literal>None</literal>, то позволено соединяться только с базами данных,
                перечисленными в файле <filename>databases.conf</filename>.</para>

            <example>
                <title>Значения параметра DatabaseAccess</title>

                <para>
                    <programlisting>
DatabaseAccess = None
DatabaseAccess = Restrict C:\DataBase
DatabaseAccess = Restrict C:\DataBase;D:\Mirror
DatabaseAccess = Restrict /db
DatabaseAccess = Restrict /db;/mnt/mirrordb
DatabaseAccess = Full              
                </programlisting>
                </para>
            </example>

            <warning>
                <para>Неконтролируемый доступ к файлам баз данных может поставить под угрозу
                    безопасность вашей системы. Поэтому настоятельно рекомендуем ограничивать
                    директории для размещения баз данных.</para>
            </warning>
        </section>

        <section xml:id="config-param-RemoteAccess">
            <title>RemoteAccess</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteAccess</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Boolean.</para>
            </formalpara>

            <para>Параметр предоставляет или отменяет удалённый доступ к базам данных.
                <programlisting>
RemoteAccess = true             
                </programlisting>
            </para>

            <para>По-умолчанию <parameter>RemoteAccess</parameter> включен для всех баз данных, за
                исключением базы данных безопасности. Если вы намереваетесь использовать больше
                одной специализированной базы безопасности, то рекомендуем отключить удалённый
                доступ к ним в файле <filename>databases.conf</filename>.</para>

            <para>Для повышения безопасности можно отключить <parameter>RemoteAccess</parameter>
                глобально в файле <filename>firebird.conf</filename> и включить его для отдельных
                баз данных в <filename>databases.conf</filename>.</para>

            <para>Параметр имеет тип Boolean и может принимать значения
                    <literal>true</literal>/<literal>false</literal>,
                    <literal>1</literal>/<literal>0</literal> или
                    <literal>yes</literal>/<literal>no</literal>.</para>
        </section>

        <section xml:id="config-param-ExternalFileAccess">
            <title>ExternalFileAccess</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>ExternalFileAccess</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String (специальный формат).</para>
            </formalpara>

            <formalpara>
                <title>Синтаксис:</title>

                <para>
                    <programlisting>
ExternalFileAccess = None | Full | { Restrict &lt;<replaceable>dir_1</replaceable>&gt;[;&lt;<replaceable>dir_2</replaceable>&gt;[...;&lt;<replaceable>dir_N</replaceable>&gt;] }
                     </programlisting>
                </para>
            </formalpara>

            <para>Параметр <parameter>ExternalFileAccess</parameter> позволяет управлять правами
                размещения файлов внешних таблиц. Разрешение на доступ к внешним файлам может быть
                полным (<literal>Full</literal>), ограниченным (<literal>Restrict</literal>) или
                запрещённым (<literal>None</literal>). </para>

            <para>Параметр <parameter>ExternalFileAccess</parameter> имеет строковый тип; значение
                по умолчанию равно None — запрет на создание внешних таблиц. Для того, чтобы
                разрешить создание и доступ к внешним файлам, следует выставить значение параметра
                равным Full. Для ограничения доступа используется значение Restrict. В этом случае
                после слова Restrict указываются директории, в которых могут быть сохранены файлы
                внешних таблиц. При указании каталогов могут быть использованы как абсолютные, так и
                относительные пути. Относительные пути берутся от корневого каталога Firebird. В
                качестве разделителя директорий используется символ <quote>;</quote>.</para>
            <example>
                <title>Примеры задания значения параметра ExternalFileAccess</title>

                <para>
                    <programlisting>
ExternalFileAccess = None
ExternalFileAccess = Restrict C:\DataBase
ExternalFileAccess = Restrict C:\DataBase;D:\Mirror
ExternalFileAccess = Restrict /db;/mnt/mirrordb
ExternalFileAccess = Full              
                </programlisting>
                </para>
            </example>

            <warning>
                <para>Неконтролируемая возможность использования внешних таблиц может поставить под
                    угрозу безопасность вашего сервера. Поэтому настоятельно рекомендуется
                    использовать этот параметр для ограничения директорий размещения внешних
                    таблиц.</para>
            </warning>
        </section>

        <section xml:id="config-param-UdfAccess">
            <title>UdfAccess</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>UdfAccess</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String (специальный формат).</para>
            </formalpara>

            <formalpara>
                <title>Синтаксис:</title>

                <para>
                    <programlisting>
UdfAccess = None | Full | { Restrict &lt;<replaceable>dir_1</replaceable>&gt;[;&lt;<replaceable>dir_2</replaceable>&gt;[...;&lt;<replaceable>dir_N</replaceable>&gt;] }
                     </programlisting>
                </para>
            </formalpara>

            <para>Параметр <parameter>UdfAccess</parameter> предназначен для определения директорий,
                в которых могут быть сохранены библиотеки UDF. Разрешение на доступ к библиотекам
                внешних функций может быть полным (Full), ограниченным (Restrict) или запрещённым
                (None).</para>
            <para>Параметр <parameter>UdfAccess</parameter> имеет строковый тип; значение по
                умолчанию равно Restrict UDF — udf-библиотеки ищутся только в корневом каталоге
                сервера в папке <filename>udf</filename>. Для того, чтобы запретить использование
                udf, нужно выставить значение параметра равным None.</para>
            <para>При указании каталогов могут быть использованы как абсолютные, так и относительные
                пути. Относительные пути берутся от корневого каталога сервера Firebird. В качестве
                разделителя директорий используется символ <quote>;</quote>.
                <programlisting>
UdfAccess = Restrict UDF           
                </programlisting>
            </para>

            <warning>
                <para>Неконтролируемая возможность использования внешних функций может быть
                    использована для того, чтобы поставить под угрозу безопасность как баз данных,
                    так и всей системы. Поэтому настоятельно рекомендуется использовать данный
                    параметр для ограничения директорий размещения udf-библиотек.</para>
            </warning>

            <note>
                <para>Этот параметр никак не влияет на правила размещения нового типа внешних
                    библиотек UDR.</para>
            </note>

        </section>

        <section xml:id="config-param-TempDirectories">
            <title>TempDirectories</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TempDirectories</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String (специальный формат).</para>
            </formalpara>

            <para>С помощью параметра <parameter>TempDirectories</parameter> можно задать временный
                каталог сервера Firebird. Временный каталог необходим для выгрузки данных во время
                сортировки (и в некоторых других случаях), когда исчерпывается выделенная
                оперативная память.</para>
            <para>Параметр <parameter>TempDirectories</parameter> имеет строковый тип; значение по
                умолчанию равно пустой строке. Если параметр <parameter>TempDirectories</parameter>
                не активен, то путь к временному каталогу определяется исходя из значения переменных
                окружения <envar>FIREBIRD_TMP</envar>, <envar>TEMP</envar>, <envar>TMP</envar>. Если
                никакая из вышеперечисленных переменных не задана, то значением для POSIX будет
                каталог <filename>/tmp</filename>, а для Windows —
                <filename>C:\TEMP</filename>.</para>
            <para>В качестве значения параметра может быть задан путь к одному или нескольким
                каталогам. В этом случае выгрузка временных данных при сортировке будет
                осуществляться в указанные каталоги. Для папок допускаются как абсолютные, так и
                относительные пути. Относительные пути берутся от корневого каталога инсталляции
                сервера Firebird. Если требуется определить несколько временных каталогов, то в
                качестве разделителя используется символ <quote>;</quote>.</para>
            <para>Если указана одна или несколько директорий, то выгрузка временных данных при
                сортировке будет осуществляться в указанные каталоги по очереди (если в текущей
                временной директории не осталось места, то временные файлы будут сохраняться в
                следующую по списку).</para>

            <example>
                <title>Примеры задания значения параметра TempDirectories</title>

                <para>
                    <programlisting>
TempDirectories = c:\temp
TempDirectories = c:\temp;d:\temp            
                </programlisting>
                </para>
            </example>

            <para>Количество ресурсов, выделяемых под <parameter>TempDirectories</parameter>,
                следует увеличивать, если планируется восстановление базы (restore), имеющей большие
                индексы. Иначе могут возникнуть проблемы с нехваткой места для
                    <filename>fb_sort*</filename> файлов, создаваемых СУБД в процессе построения
                индексов (СУБД не будет запрашивать у операционной системы новое место в этом
                случае).</para>

            <note>
                <title>Замечания</title>
                <itemizedlist>
                    <listitem>
                        <para>Данные GTT (глобальных временных таблиц) хранятся в файлах с маской
                                <filename>fb_table*</filename>, создаваемых в каталогах, заданных
                            переменными <envar>FIREBIRD_TMP</envar> (затем — <envar>TMP</envar>; при
                            отсутствии их обоих GTT-данные будут сохраняться — для POSIX — в
                            каталоге <filename>/tmp</filename>). СУБД при определении места, где
                            надо хранить GTT-данные, не учитывает значение параметра
                                <parameter>TempDirectories</parameter>. Поэтому правильным методом
                            является назначение переменной <envar>FIREBIRD_TMP</envar> того же
                            каталога, что указан в <parameter>TempDirectories</parameter>.</para>
                    </listitem>
                    <listitem>
                        <para>В POSIX временные файлы, создаваемые СУБД
                                (<filename>fb_table*</filename>, <filename>fb_sort*</filename>,
                                <filename>fb_recbuf*</filename>, <filename>fb_blob*</filename>,
                                <filename>fb_merge*</filename>), сразу маркируются как
                                <quote>deleted</quote> и не видны при обычном просмотре каталога.
                            Для получения их списка воспользуйтесь, например, пакетом
                                <application>lsof</application> с указанием в качестве аргумента PID
                            процесса Firebird (если он работает как SuperServer или
                            SuperClassic).</para>
                    </listitem>
                    <listitem>
                        <para>В следующем примере процесс firebird имеет PID = 9447, и для получения
                            открытых им временных файлов следует ввести команду:</para>
                        <synopsis>
#lsof -p 9447 | grep "/fb_table\|/fb_sort\|/fb_recbuf\|/fb_blob\|/fb_merge"                            
                        </synopsis>
                        <para>Пример вывода:</para>
                        <screen>
firebird 9447 firebird  296u REG  253,0    147456  2490547 /tmp/fb_table_5aNN2s 
  (deleted)
firebird 9447 firebird 1503u REG  253,0    147456  2490573 /tmp/fb_table_0Jwjo3 
  (deleted)
firebird 9447 firebird  623u REG   0,16  75497472 25189649 /tmp/fb_sort_kv21Av 
  (deleted)
firebird 9447 firebird  296u REG  253,0    147456  2490547 /tmp/fb_table_5aNN2s 
  (deleted)
                        </screen>
                    </listitem>
                    <listitem>
                        <para>В Windows временные файлы видны, но их равен нулю до тех пор, пока
                            остается свободная память, заданные параметром
                                <parameter>TempCacheLimit</parameter>.</para>
                    </listitem>
                </itemizedlist>
            </note>
        </section>

        <section xml:id="config-param-AuditTraceConfigFile">
            <title>AuditTraceConfigFile</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>AuditTraceConfigFile</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>AuditTraceConfigFile</parameter> в файле конфигурации
                    <filename>firebird.conf</filename> задает имя и расположение файла с настройками
                системного аудита. Этот параметр имеет строковый тип и по умолчанию имеет пустое
                значение. Пустое значение параметра означает, что системный аудит выключен.</para>

            <para>Указанный файл конфигурации должен иметь ту же структуру, что и
                    <filename>fbtrace.conf</filename>.</para>

            <example>
                <title>Примеры задания значения параметра AuditTraceConfigFile</title>

                <para>
                    <programlisting>
AuditTraceConfigFile = fbtrace.conf         
                </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-MaxUserTraceLogSize">
            <title>MaxUserTraceLogSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>MaxUserTraceLogSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Задает максимальный суммарный размер (в мегабайтах) временных файлов, создаваемых
                сессией пользовательской трассировки. После прочтения временного файла приложением
                он автоматически удаляется. Параметр имеет целочисленный тип. Единица измерения —
                мегабайты. По умолчанию максимальный размер файла вывода ограничен 10 МБ. Если
                значения ограничения <parameter>MaxUserTraceLogSize</parameter> достигнуто, то
                сервер автоматически приостанавливает сессию слежения.</para>

            <example>
                <title>Примеры задания значения параметра MaxUserTraceLogSize</title>

                <para>
                    <programlisting>
MaxUserTraceLogSize = 10         
                </programlisting>
                </para>
            </example>
            <note>
                <para>Количество пользовательских сессий трассировки, которые можно запускать на
                    одном сервере, ничем не ограничено. Указанный параметр лимитирует размер файлов
                    для каждой отдельной сессии трассировки, а не для всех сеансов. Поэтому общий
                    размер временных файлов может превышать 10 Мб.</para>
            </note>
        </section>

        <section xml:id="config-param-DefaultDbCachePages">
            <title>DefaultDbCachePages</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>DefaultDbCachePages</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>DefaultDbCachePages</parameter> используется для настройки
                количества страниц одной базы данных, находящихся в кеш-памяти одновременно.
                SuperServer использует единый страничный кеш (2048 страниц по умолчанию) для всех
                подключений. Classic и SuperClassic создает отдельный страничный кеш (по умолчанию
                256 страниц) для каждого соединения.</para>

            <para>Параметр имеет целочисленный тип. Допустимо использовать множители (k, m, g).
                Максимальное значение 2147483647 страниц.</para>
            <para>Этот параметр может быть установлен для каждой базы данных индивидуально в файле
                    <filename>databases.conf</filename>. Это особенно удобно если на вашем сервере
                находятся несколько разных баз данных под управлением одного экземпляра
                Firebird.</para>

            <para>Размер страничного кеша может быть установлен на странице заголовков файла базы
                данных с помощью утилиты gfix.
                <programlisting>
gfix -b[uffers] <replaceable>&lt;page-count&gt;</replaceable> <replaceable>&lt;database&gt;</replaceable>                   
                </programlisting>
            </para>

            <para>В этом случае размер страничного кеша устанавливается для каждой базы
                одновременно. На наш взгляд предпочтительно делать это в файле
                    <filename>databases.conf</filename>, где вы можете установить и другие параметры
                индивидуальные для вашей базы данных.</para>

            <para>Обратите внимание, размер страничного кеша установленный на уровне заголовка базы
                данных перекрывает значение установленное в <filename>firebird.conf</filename> и
                    <filename>databases.conf</filename>.</para>

            <tip>
                <para>Чтобы проверить установлен ли размер страничного кеша на уровне заголовка базы
                    данных вы можете воспользоваться утилитой gstat <programlisting>
d:\fb\fb30>gstat -u sysdba -p masterkey -h testdb               
                </programlisting>
                    <screen>
Database "d:\fb\fb30\data\testdb.fdb"
Gstat execution time Wed Jul 17 12:59:26 2019

Database header page information:
        Flags                   0
        Generation              2180
        System Change Number    0
        Page size               16384
        ODS version             12.0
        Oldest transaction      2037
        Oldest active           2038
        Oldest snapshot         2038
        Next transaction        2038
        Sequence number         0
        Next attachment ID      391
        Implementation          HW=AMD/Intel/x64 little-endian OS=Windows CC=MSV
C
        Shadow count            0
        Page buffers            0
        Next header page        0
        Database dialect        1
        Creation date           Jun 28, 2016 18:04:35
        Attributes              force write

    Variable header data:
        Sweep interval:         20000
        *END*
Gstat completion time Wed Jul 17 12:59:26 2019                     
                    </screen>
                </para>

                <para>Page buffers — размер кэша базы данных. Если это значение равно 0, то будет
                    использоваться значние параметра <parameter>DefaultDbCachePages</parameter> в
                        <filename>firebird.conf</filename> или
                    <filename>databases.conf</filename>.</para>
            </tip>

            <para>При изменении данного параметра стоит учитывать архитектуру сервера, максимальное
                количество соединений с базой данных (Classic, SuperClassic), количество доступной
                оперативной памяти, а также некоторые другие настройки сервера.</para>
            <para>В архитектуре Classic количество страниц может управляться отдельным подключением
                (то есть оно само может сказать, сколько ему сейчас нужно страниц). Размер
                страничного кеша может быть увеличен только после рестарта Firebird (для архитектуры
                SuperServer) или переподключения (для Classic).</para>
            <para>В архитектуре SuperServer (предоставляющей для всех подключений общий страничный
                кеш) рекомендуется назначать страничный кеш как можно большим, но также есть
                рекомендация не делать его размер более, чем половина от физического размера ОЗУ.
                Для очень больших БД достаточно выделить такой размер кеша, чтобы в него помещалась
                активная часть БД. Достаточность кеша обычно проверяют соотношением read/fetch.
                Хорошим соотношением для FB является <code>read/fetch &lt; 0.005</code>.</para>

            <example>
                <title>Установка параметра DefaultDbCachePages в firebird.conf</title>

                <para>
                    <programlisting>
DefaultDbCachePages = 2048   
                    </programlisting>
                </para>
            </example>

            <example>
                <title>Установка параметра DefaultDbCachePages в databases.conf для базы данных
                    testdb</title>

                <para>
                    <programlisting>
testdb = d:\fb\fb30\data\testdb.fdb
{
    DefaultDbCachePages = 8K
}   
                    </programlisting>
                </para>
            </example>

            <para>Кроме того, следует обращать внимание на значение параметра
                    <parameter>FileSystemCacheThreshold</parameter>. Если размер страничного кеша
                превышает значение параметра <parameter>FileSystemCacheThreshold</parameter>, то
                системный файловый кеш будет отключен. </para>


            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-FileSystemCacheThreshold"
                        >FileSystemCacheThreshold</link></para>
            </formalpara>
        </section>

        <section xml:id="config-param-DatabaseGrowthIncrement">
            <title>DatabaseGrowthIncrement</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>DatabaseGrowthIncrement</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр позволяет указать объем дискового пространства, которое может быть
                предварительно выделено под базу данных. Дисковое пространство резервируется в
                системе, что позволяет в дальнейшем снизить физическую фрагментацию файла (-ов) базы
                данных и дает возможность продолжить работу в условиях недостатка места на диске.
                Если режим резервирования включен, то сервер резервирует 1/16 часть от уже
                используемого дискового пространства для одного соединения, но не меньше 128 KB и не
                больше, чем значение, заданное параметром
                    <parameter>DatabaseGrowthIncrement</parameter> (по умолчанию 128 MB).</para>

            <para>Для отключения резервирования дискового пространства необходимо выставить значение
                    <parameter>DatabaseGrowthIncrement</parameter> равным 0.</para>

            <example>
                <title>Установка параметра DatabaseGrowthIncrement</title>

                <para>
                    <programlisting>
DatabaseGrowthIncrement = 128M   
                    </programlisting>
                </para>
            </example>

            <note>
                <para>Пространство под теневые копии баз данных не резервируется.</para>
            </note>
        </section>
        <section xml:id="config-param-FileSystemCacheThreshold">
            <title>FileSystemCacheThreshold</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>FileSystemCacheThreshold</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>FileSystemCacheThreshold</parameter> устанавливает порог
                использования системного файлового кэша сервером Firebird. Системный файловый кэш
                используется, если размер страничного кэша (установленного явно в заголовке базы
                данных или через параметр конфигурации <parameter>DefaultDbCachePages</parameter>)
                меньше чем значение <parameter>FileSystemCacheThreshold</parameter>.</para>

            <para>Параметр имеет целочисленный тип. Единица измерения – страница базы данных. По
                умолчанию параметр имеет значение — 65536 страниц. Максимально допустимое значение
                параметра — 2147483647. Минимальное значение параметра — 0. Если значение параметра
                FileSystemCacheThreshold равно 0, то сервер не будет использовать системный файловый
                кэш. </para>

            <example>
                <title>Установка параметра FileSystemCacheThreshold</title>

                <para>
                    <programlisting>
FileSystemCacheThreshold = 128K  
                    </programlisting>
                </para>
            </example>

            <note>
                <para>Системный файловый кэш имеет смысл отключать только при достаточно большом
                    размере страничного кэша базы данных. Это позволяет избегать двойного
                    кэширования и снизить потребление оперативной памяти. Это может быть полезным
                    для баз, работающих в режиме read-only на архитектуре SuperServer. Для баз
                    данных, работающих в режиме OLTP-нагрузки, отказ от кеширования файловой
                    системой почти всегда приводит к крайне сильной просадке производительности.
                    Одна из причин — отсутствие в СУБД Firebird возможности упреждающего чтения и
                    отложенной записи, которые есть в операционной системе. Для таких баз всегда
                    должно соблюдаться правило: <code>DefaultDbCachePages &lt;
                        FileSystemCacheThreshold</code>.</para>
            </note>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-DefaultDbCachePages"
                    >DefaultDbCachePages</link></para>
            </formalpara>
        </section>
        <section xml:id="config-param-FileSystemCacheSize">
            <title>FileSystemCacheSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>FileSystemCacheSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer, измеряется в процентах от объёма доступной оперативной памяти.</para>
            </formalpara>

            <para>Параметр <parameter>FileSystemCacheSize</parameter> устанавливает максимальный
                размер оперативной памяти, используемый системным файловым кешем 64-битными Windows
                XP или Windows Server 2003 с Service Pack 1 или выше. Этот параметр не оказывает
                никакого эффекта в Unix-подобных операционных системах.</para>
            <para>Параметр содержит целое число, представляющее собой количество (в процентах)
                оперативной памяти, которое может быть использовано под файловый кеш. Значение может
                быть от 10 до 95%.</para>
            <para>Значение считывается при старте сервера и не может быть изменено. Требуется
                перезагрузка операционной системы для вступления изменений в силу. </para>
            <para>Если задать значение 0, операционная система сама будет определять размер
                файлового кеша (обычно это 30%). Это и есть значение по умолчанию. </para>

            <example>
                <title>Установка параметра FileSystemCacheSize</title>

                <para>
                    <programlisting>
FileSystemCacheSize = 25  
                    </programlisting>
                </para>
            </example>

            <note>
                <para>Windows требует обладания привилегией SeIncreaseQuotaPrivilege для управления
                    настройками файлового кеша. Эта привилегия доступна по умолчанию администраторам
                    и службам, а также выдается учетной записи Firebird при установке из
                    дистрибутива Windows Installer.</para>

                <para>Если Firebird запущен как приложение или в режиме Embedded или установлен не
                    из официального дистрибутива, учетная запись может не иметь данной привилегии.
                    Процесс не выдаст ошибку при запуске, а просто запишет соответствующее сообщение
                    в файл <filename>firebird.log</filename> и будет работать с настройками
                    операционной системы.</para>
            </note>
        </section>

        <section xml:id="config-param-RemoteFileOpenAbility">
            <title>RemoteFileOpenAbility</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteFileOpenAbility</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Boolean.</para>
            </formalpara>

            <para>Параметр <parameter>RemoteFileOpenAbility</parameter> отключает защиту от открытия
                баз данных на смонтированных томах NFS в Linux/Unix и SMB/CIFS в Windows.</para>
            <warning>
                <para>Эта опция удаляет важную функцию безопасности Firebird и может привести к
                    неисправимому повреждению базы данных. Не используйте эту опцию, если вы не
                    понимаете риски и не готовы принять потерю содержимого вашей базы данных.</para>
            </warning>
            <para>Параметр имеет логический тип. По умолчанию его значение равно 0. В этом случае
                Firebird может открыть базу данных, только если она хранится на диске, физически
                подключенном к локальному компьютеру, на котором установлена ​​эта копия Firebird.
                Запросы на подключение к базам данных, хранящимся на смонтированных дисках NFS,
                перенаправляются на сервер Firebird, работающий на компьютере, который
                    <quote>владеет</quote> диском.</para>
            <example>
                <title>Установка параметра RemoteFileOpenAbility</title>

                <para>
                    <programlisting>
RemoteFileOpenAbility = 0  
                    </programlisting>
                </para>
            </example>
            <para>Это ограничение не позволяет двум разным копиям Firebird открывать одну и ту же
                базу данных без координации их действий. Несогласованный доступ нескольких копий
                Firebird приведет к повреждению базы данных. В локальной системе блокировка файлов
                на уровне системы предотвращает несогласованный доступ к файлу базы данных.</para>
            <para>NFS не обеспечивает надежный способ обнаружения доступа нескольких пользователей к
                файлу на смонтированном диске NFS. Если вторая копия Firebird подключится к базе
                данных на смонтированном диске NFS, она повредит базу данных. В некоторых случаях
                запуск сервера Firebird на компьютере, который владеет томами, смонтированными в
                NFS, неудобен или невозможен. Приложения, которые используют embedded вариант
                Firebird и никогда не разделяющие доступ к базе данных, могут использовать эту
                опцию, чтобы разрешить прямой доступ к базам данных на смонтированных томах
                NFS.</para>
            <para>Ситуация для SMB / CIFS очень похожа на NFS, поскольку не все конфигурации
                предоставляют механизмы блокировки файлов, необходимые для безопасной работы.
                Использование механизма SuperServer с базой данных на файловом сервере NT может
                считаться относительно безопасным, поскольку блокировка файлов защищает базу данных
                от использования несколькими механизмами. Сетевой стек по-прежнему может изменять
                порядок записи, поэтому вы можете получить поврежденную базу данных в случае сетевых
                ошибок или отключения питания. </para>
            <para>Относительно полезный и безопасный случай — это работа с общей базой данных,
                помеченной только для чтения. </para>
        </section>

        <section xml:id="config-param-TempBlockSize">
            <title>TempBlockSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TempBlockSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>TempBlockSize</parameter> используется для управления
                временным пространством. Временное хранилище используются большими сортировками, или
                для промежуточного хранения набора данных. Параметр
                    <parameter>TempBlockSize</parameter> определяет размер блока, выделяемого для
                временного хранилища. Это значение отражает гранулярность выделения
                пространства.</para>

            <para>Параметр имеет целочисленный тип. Единица измерения — байты. По умолчанию параметр
                имеет значение 1 Мбайт. Максимально допустимое значение 2 Гбайт. Минимальное
                значение параметра — 0.</para>

            <example>
                <title>Установка параметра TempBlockSize</title>

                <para>
                    <programlisting>
TempBlockSize = 2M  
                    </programlisting>
                </para>
            </example>

            <para>Оптимальное значение этого параметра (хотя и трудно определяемое) — средний размер
                памяти, отводимой на операцию сортировки.</para>

            <para>Предельный размер всей области оперативной памяти, выделяемой для временных
                файлов, задается параметром <parameter>TempCacheLimit</parameter>. Блоки, которые
                ранее выделялись, хранится в нём как линейный список, поиск в котором выполняется
                методом половинного деления (бисекции). Если <parameter>TempCacheLimit</parameter>
                задан слишком большого размера, например, 16 Гб, то даже при его заполненности на
                50% (т.е. 8Гб) для поиска блока размером 1 Мб потребуется 13 шагов. В этом случае
                лучше увеличить размер <parameter>TempBlockSize</parameter> до 3-4М, что приведет к
                уменьшению числа шагов бисекции до 6-8. В то же время, рекомендуется ставить
                    <parameter>TempBlockSize</parameter> в значение, НЕ превышающее 5% от
                    <parameter>TempCacheLimit</parameter>, а также оно должно быть НЕ меньше 1
                Мб.</para>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-TempCacheLimit">TempCacheLimit</link></para>
            </formalpara>
        </section>

        <section xml:id="config-param-TempCacheLimit">
            <title>TempCacheLimit</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TempCacheLimit</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Int64.</para>
            </formalpara>

            <para>Параметр <parameter>TempCacheLimit</parameter> определяет максимальный объём
                оперативной памяти, который используется для кэширования временного
                пространства.</para>
            <para>Параметр имеет целочисленный тип. Единица измерения — байты. Минимальное значение
                равно 0, максимальное — (2<superscript>64</superscript> - 1) байт. Значение по
                умолчанию для классического сервера равно 8 Мбайт, для архитектур SuperClassic и
                SuperServer — 64 Мбайт.</para>

            <important>
                <para>Для классического сервера это ограничение распространяется для каждого
                    соединения с базой данных, для SuperServer и SuperClassic — для каждого
                    запущенного экземпляра сервера Firebird.</para>
            </important>

            <example>
                <title>Установка параметра TempCacheLimit</title>

                <para>
                    <programlisting>
TempCacheLimit = 256M  
                    </programlisting>
                </para>
            </example>

            <note>
                <itemizedlist>
                    <listitem>
                        <para>Firebird 3.0 и выше позволяет указывать значения этого параметра
                            больше, чем 4 Гб.</para>
                    </listitem>
                    <listitem>
                        <para>Когда какому-то соединению память перестает быть нужной, она
                            возвращается операционной системе. Другими словами, задание
                                <code>TempCacheLimit = 4096M</code> не означает, что 4 Гб памяти
                            будут сразу и безвозвратно зарезервированы процессом Firebird.</para>
                    </listitem>
                    <listitem>
                        <para>Увеличение <parameter>TempCacheLimit</parameter> даёт лучший эффект
                            чем назначение для временных файлов каталога на tmpfs (см. параметр
                                <parameter>TempDirectories</parameter>), так как при этом не
                            возникает дополнительных расходов на обмен данными с ядром операционной
                            системы.</para>
                    </listitem>
                    <listitem>
                        <para>Значение <parameter>TempCacheLimit</parameter> гарантирует, что данные
                            этого размера будут находиться в оперативной памяти. Когда память,
                            отведенная для временного хранения данных, будет исчерпана, очередной
                            запрос от Firebird на получение блока размером
                                <parameter>TempBlockSize</parameter> байт приведет к тому, что новая
                            память может быть уже выделена на диске в swap-области.</para>
                    </listitem>
                    <listitem>
                        <para>В Firebird 3.0 в области <parameter>TempCacheLimit</parameter>
                            размещаются данные при выполнении hash-соединений.</para>
                    </listitem>
                </itemizedlist>
            </note>

            <warning>
                <para>Будьте осторожны с назначением этого параметра при работе СУБД в режиме
                    Classic Server. Общий лимит памяти, которую Firebird будет запрашивать у
                    операционной системы для временных файлов, определяется при этом как результат
                    умножения <parameter>TempCacheLimit</parameter> на количество
                    подключений.</para>
            </warning>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-TempBlockSize">TempBlockSize</link></para>
            </formalpara>
        </section>

        <section xml:id="config-param-Auth">
            <title>AuthServer и AuthClient</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>AuthServer</secondary>
            </indexterm>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>AuthClient</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>AuthServer</parameter> — набор методов аутентификации,
                разрешенных на сервере (определяется в файле конфигурации сервера).</para>
            <para>Параметр <parameter>AuthClient</parameter> — набор методов аутентификации,
                поддерживаемых клиентом (определяется в файле конфигурации на клиенте).</para>
            <para>Включенные методы перечислены в виде строковых символов, разделенных запятыми,
                точками с запятой или пробелами. Если проверить подлинность с помощью первого метода
                не удалось, то сервер переходит к следующему и т.д. Если ни один метод не подтвердил
                подлинность, то пользователь получает сообщение об ошибке.</para>
            <para>Firebird поддерживает следующие методы аутентификации:<itemizedlist>
                    <listitem>
                        <para>Безопасная парольная аутентификация (Srp). Протокол SRP (Secure Remote
                            Password) позволяет пользователю, не передавая своего пароля,
                            подтвердить серверу тот факт, что он знает свой пароль;</para>
                    </listitem>
                    <listitem>
                        <para>Традиционная (Legacy_Auth) аутентификация, использовалась в качестве
                            основного метода аутентификации в Firebird версии 2.5 и ниже;</para>
                    </listitem>
                    <listitem>
                        <para>Доверительная (Win_Sspi) аутентификация для ОС Windows.</para>
                    </listitem>
                </itemizedlist></para>


            <para>По умолчанию на стороне сервера используется метод Secure remote passwords
                (Srp256), представленный соответствующей ОС плагином (libSrp.so | Srp.dll |
                Srp.dylib).
                <programlisting>
AuthServer = Srp256            
            </programlisting>
            </para>

            <note>
                <para>Первоначально Firebird 3.0 использовал по умолчанию плагин Srp, который
                    использовал SHA-1 для генерации ключа шифрования, начиная с Firebird 3.0.4
                    введён усовершенствованный метод аутентификации Srp256, который использует
                    SHA-256.</para>
            </note>

            <para>Список AuthServer по умолчанию содержит только один элемент (Srp). Это значит, что
                к такому серверу возможно подключение только тех приложений, которые используют
                клиента версии 3 и старше. Если ваш сервер должен принимать подключения от клиентов
                версий до 2.5 включительно, необходимо добавить в этот список элемент
                Legacy_Auth.</para>

            <note>
                <para>Традиционная (Legacy_Auth) не умеет работать с новым менеджером пользователей
                    Srp. Поэтому если вы хотите соединяться с Firebird 3.0 клиентами fbclient
                    версией 2.5 и ниже, то необходимо также включить в список плагинов управления
                    пользователями (параметр UserManager) плагин Legacy_UserManager.</para>
            </note>

            <para>Если вы хотите использовать плагины аутентификации, которые не предоставляют ключа
                шифрования (Win_Sspi, Legacy_Auth), то следует отключить обязательное (Required)
                шифрование каналов передачи данных (параметр <parameter>WireCrypt</parameter>),
                кроме случаев, когда вы работаете с протоколом XNET, который никогда не использует
                шифрование.</para>

            <para>На клиентской стороне по умолчанию используется следующий список методов
                аутентификации:
                <programlisting>
AuthClient = Srp256, Srp, Win_Sspi, Legacy_Auth  # для Windows клиентов     
AuthClient = Srp256, Srp, Legacy_Auth            # для не Windows клиентов 
            </programlisting>
            </para>

            <para>Под клиентом следует понимать как обычное подключение от пользователя, так и
                запрос от сервера-1, к которому ранее уже было подключение, к серверу-2, через
                механизм external datasource. В этом случае клиентом выступает сервер-1.</para>


            <para>Плагин Legacy_Auth присутствует в списке AuthClient по умолчанию для того, чтобы к
                клиенты версии 3.0 и выше могли подключаться к серверам более старых версий. </para>


            <para>Чтобы отключить какой-нибудь из методов, раскомментируйте строку и удалите
                нежелательный метод из списка.</para>

            <tip>
                <para>Поскольку плагины аутентификации пробуются в порядке их перечисления, то для
                    наилучшей производительности переместите наиболее часто используемый метод
                    аутентификации на первое место.</para>
            </tip>

            <para>Оба параметра могут быть использованы в <filename>databases.conf</filename>. Они
                могут использоваться как в DPB, так и в SPB для конкретных настроек
                соединения.</para>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-UserManager">UserManager</link>, <link
                        linkend="config-param-WireCrypt">WireCrypt</link>.</para>
            </formalpara>

        </section>

        <section xml:id="config-param-UserManager">
            <title>UserManager</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>UserManager</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Устанавливает плагин, который будет работать в базе данных безопасности. Это может
                быть список с пробелами, запятыми или точками с запятой в качестве разделителей.
                По-умолчанию используется первый подключаемый плагин из списка. Firebird
                поддерживает следующие плагины управления пользователями: <itemizedlist>
                    <listitem>
                        <para>Srp;</para>
                    </listitem>
                    <listitem>
                        <para>Legacy_UserManager.</para>
                    </listitem>
                </itemizedlist></para>

            <para>Для поддержки старой базы данных безопасности и управления пользователями в ней,
                следует установить значение параметра Legacy_UserManager.</para>

            <example>
                <title>Установка параметра UserManager</title>

                <para>
                    <programlisting>
UserManager = Srp  
                    </programlisting>
                </para>
            </example>

            <para>В SQL операторах управления пользователями можно явно указать какой плагин будет
                использоваться с помощью ключевого слова USING.</para>

            <note>
                <para>Одноименные пользователи, созданные с помощью разных плагинов управления
                    пользователями — это разные пользователи.</para>
            </note>

            <para>Параметр <parameter>UserManager</parameter> можно использовать в
                    <filename>database.conf</filename> для переопределения в конкретной базе
                данных.</para>
        </section>

        <section xml:id="config-param-TracePlugin">
            <title>TracePlugin</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TracePlugin</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Задает плагин, используемый функцией трассировки Firebird для отправки данных
                трассировки в приложение клиента или данных аудита в лог файл.</para>

            <example>
                <title>Установка параметра TracePlugin</title>

                <para>
                    <programlisting>
TracePlugin = fbtrace  
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-WireCryptPlugin">
            <title>WireCryptPlugin</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>WireCryptPlugin</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-connection.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Плагин поточного шифра используется для шифрования и дешифрования данных,
                передаваемых по сети. </para>
            <para>По-умолчанию устанавливается значение параметра Arc4, что означает использование
                плагина потокового шифра Alleged RC4. Сконфигурированный плагин, который требует
                ключ, сгенерированный настроенным подключаемым модулем аутентификации, может быть
                переопределен в API для конкретного соединения через DPB или SPB.</para>

            <example>
                <title>Установка параметра WireCryptPlugin</title>

                <para>
                    <programlisting>
WireCryptPlugin = Arc4  
                    </programlisting>
                </para>
            </example>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-WireCrypt">WireCrypt</link>.</para>
            </formalpara>
        </section>

        <section xml:id="config-param-KeyHolderPlugin">
            <title>KeyHolderPlugin</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>KeyHolderPlugin</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Этот параметр представляет собой некоторую форму временного хранилища для ключей
                шифрования базы данных.</para>

            <para>Реализованного плагина по-умолчанию нет, но пример для Linux под названием
                    <filename>libCryptKeyHolder_example.so</filename> можно найти в папке
                    <filename>/plugins/</filename>.</para>
        </section>

        <section xml:id="config-param-AllowEncryptedSecurityDatabase">
            <title>AllowEncryptedSecurityDatabase</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>AllowEncryptedSecurityDatabase</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Boolean.</para>
            </formalpara>

            <para>Задает возможность использования зашифрованной базы данных с учетными записями
                пользователей и паролями.</para>

            <para>Передача по зашифрованному сетевому каналу с ключем шифрования, который создается
                плагином аутентификации (Srp) для передачи далее по сети ключа шифрования БД, может
                рассматриваться как замкнутый круг. Чтобы послать ключ шифрования БД по сети
                безопасным способом, передаваемые части ключа уже должны быть зашифрованными. Но это
                требует наличия ключа шифрования от плагина аутентификации, которому нужно открыть
                базу безопасности для валидации хеша от пароля, а это, в свою очередь, требует ключа
                шифрования БД.</para>
            <para>К счастью, в большинстве случаев нет необходимости шифровать базу данных
                безопасности — она ​​достаточно хорошо защищает себя, если вы используете пароли
                надлежащего качества. Но в некоторых случаях желательно иметь зашифрованную базу
                безопасности, например, если нужно, чтобы зашифрованная база с пользовательскими
                данными, также служила базой безопасности. В этом случае особое внимание следует
                уделить шифрованию ключа перед передачей его на сервер с помощью обратного
                вызова.</para>
            <para>Убедитесь, что ваши ключи хорошо зашифрованы перед тем, как разрешать шифрование
                базы безопасности. Примите по внимание, что при AllowEncryptedSecurityDatabase=TRUE
                возможна передача ключа, который не зашифрован протоколом Firebird, даже при не
                зашифрованной базе безопасности.</para>
            <para>Указанное свойство не поддерживается унаследованным (Legacy) плагином проверки
                пользователей. Если вам нужна безопасность, пожалуйста, никогда не используйте
                унаследованную аутентификацию.</para>
            <warning>
                <para>Eбедитесь в том, что понимаете свои действия, прежде чем менять этот параметр
                    на True.</para>
            </warning>
        </section>

        <section xml:id="config-param-Providers">
            <title>Providers</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>Providers</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database и per-connection.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String.</para>
            </formalpara>

            <para>Провайдеры — это практически то, что мы подразумеваем под способами, используемыми
                для соединения клиента с сервером, т.е. через интернет; на том же компьютере через
                'localhost'; или через прямое соединение в локальной сети (старый
                    <filename>libfbembed.so</filename> для POSIX сейчас реализован как библиотека
                    <filename>libEngine12.so</filename>; для Windows —
                    <filename>engine12.dll</filename>; для MacOSX —
                    <filename>engine12.dylib</filename>).</para>
            <para>В <filename>firebird.conf</filename> доступны по-умолчанию следующие провайдеры:
                <programlisting>
Providers = Remote,Engine12,Loopback            
            </programlisting>
            </para>
            <para>В <filename>databases.conf</filename> один или несколько провайдеров могут быть
                заблокированы, если вставить и раскомментировать строку из
                    <filename>firebird.conf</filename> и удалить нежелательные провайдеры.</para>
            <para>Архитектура провайдеров (известная как Open Systems Relational Interface, OSRI)
                очень эффективна для поддержки сочетания старых и новых форматов базы данных (с
                разными ODS) на одном сервере, имеющих смешанные подключения к локальным и удаленным
                базам данных.</para>
            <para>Провайдеры реализованные в Firebird, позволяют поддерживать все эти режимы
                (удаленные соединения, базы данных с разными ODS), а также цепочки провайдеров.
                Цепочка — это термин для ситуации, когда провайдер использует обратный вызов
                стандартного API для выполнения операции над базой данных.</para>

            <para>Главным элементом архитектуры провайдеров является <emphasis role="bold"
                    >y-valve</emphasis>. На начальном этапе соединения с базой данных или её
                создания <emphasis role="bold">y-valve</emphasis> просматривает список известных
                провайдеров и вызывает их по одному, пока один из них не завершит запрошенную
                операцию успешно. Для соединения, которое уже установлено, соответствующий провайдер
                вызывается сразу с почти нулевыми накладными расходами.</para>

            <para>Рассмотрим пример работы <emphasis role="bold">y-valve</emphasis>, когда он
                выбирает подходящего провайдера при подключении к базе данных. Конфигурация
                по-умолчанию содержит три провайдера:<itemizedlist>
                    <listitem>
                        <para><emphasis role="bold">Remote</emphasis> (используется для сетевого
                            соединения);</para>
                    </listitem>
                    <listitem>
                        <para><emphasis role="bold">Engine12</emphasis> (ядро для работы с ODS
                            12);</para>
                    </listitem>
                    <listitem>
                        <para><emphasis role="bold">Loopback</emphasis> (принудительное соединение с
                            локальным сервером, когда задано только имя базы данных без явного
                            указания протокола).</para>
                    </listitem>
                </itemizedlist></para>

            <para>Типичная конфигурация клиента работает таким образом: при подключении к базе
                данных с именем RemoteHost:dbname (синтаксис TCP/IP) или \\RemoteHost\dbname
                (NetBios) или в URI-подобном синтаксисе с протоколами inet и wnet, провайдер Remote
                обнаруживает явный синтаксис сетевого протокола и перенаправляет вызов
                RemoteHost.</para>

            <para>Когда <replaceable>&lt;database name&gt;</replaceable> не содержит сетевого
                протокола, а только имя базы данных, провайдер Remote отклоняет его, а провайдер
                Engine12 выходит на первый план и пытается открыть файл с именованной базой данных.
                Если это проходит успешно, создается подключение к базе данных.</para>

            <para>Но что происходит, если СУБД возвращает ошибку при попытке подключения к базе
                данных? <itemizedlist>
                    <listitem>
                        <para>Если файл базы данных, к которому нужно подключиться, не существует,
                            то это не интересно всем.</para>
                    </listitem>
                    <listitem>
                        <para>Встроенное соединение может не работать, если пользователь,
                            подключившийся к нему, не имеет достаточных прав для открытия файла базы
                            данных. Это было бы обычной ситуацией, если бы база данных не была
                            создана этим пользователем во встроенном режиме или если ему явно не
                            были предоставлены права ОС на встроенный доступ к базам данных.</para>

                        <note>
                            <para>Это также может происходить, если выбран режим SuperServer и уже
                                существует соединение с базой данных, поскольку SuperServer требует
                                эксклюзивного доступа к файлу базы данных.</para>
                        </note>
                    </listitem>
                    <listitem>
                        <para>После отказа провайдера Engine12 в получении доступа к базе данных,
                            пытается подключиться провайдер Loopback. Он не очень отличается от
                            Remote, за исключением того, что он пытается получить доступ к
                            именованной базе данных <replaceable>&lt;dbname&gt;</replaceable> на
                            сервере c сетевым интерфейсом <quote>внутренней петли</quote> (loopback)
                            в сетевом протоколе TCP/IP.</para>
                        <para>В Windows XNET пробуется первым, затем TCP/IP loopback
                                (localhost:<replaceable>&lt;dbname&gt;</replaceable>), затем Named
                            Pipes (NetBEUI) loopback. Сервер может быть запущен с отключенным XNET
                            (или любым другим протоколом), поэтому перебираются все варианты. В
                            POSIX поддерживается только TCP/IP протокол, остальные варианты не
                            доступны.</para>
                    </listitem>
                </itemizedlist></para>

            <note>
                <para>Если вы хотите добиться поведения, аналогичного Firebird 2.5 и ниже, когда при
                    наличии запущенного сервера и указании в строке соединения только алиаса базы
                    данных или пути до неё, устанавливалось соединение по локальному проколу, то
                    необходимо поменять провайдеры Engine12 и Loopback местами.</para>
                <para>Для подключения по локальному протоколу рекомендуем использовать URI-подобную
                    строку подключения с явным указанием префикса протокола
                        <filename>xnet://</filename>.</para>
            </note>


            <para>Провайдеры не ограничены тремя вышеперечисленными. Версия 3.0 не поддерживает pre-
                ODS 12 провайдер. Тем не менее, архитектура провайдеров делает возможным доступ к
                старым базам данных при переходе на более высокую версию Firebird.</para>

            <note>
                <para>Firebird 4.0 для работы с ODS 13 реализован провайдер Engine13, тем не менее
                    вы можете работать с базой данных ODS 12, через провайдер Engine12 из Firebird
                    3.0.</para>
            </note>
        </section>

        <section xml:id="config-param-DeadlockTimeout">
            <title>DeadlockTimeout</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>DeadlockTimeout</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>DeadlockTimeout</parameter> определяет количество секунд, в
                течение которых диспетчер блокировок будет ожидать после возникновения конфликта,
                прежде чем очистить блокировки от мертвых процессов и выполнить дополнительный цикл
                сканирования взаимоблокировок. Firebird обнаруживает взаимные блокировки мгновенно
                во всех обычных случаях, поэтому это значение влияет на ситуации, когда что-то идет
                не так. </para>

            <para>Параметр имеет целочисленный тип. Единица измерения — секунды. Значение по
                умолчанию равно 10 секунд. Минимально допустимое значение параметра равно 0.
                Максимально допустимое значение равно 2147483647.</para>

            <example>
                <title>Установка параметра DeadlockTimeout</title>

                <para>
                    <programlisting>
DeadlockTimeout = 10 
                    </programlisting>
                </para>
            </example>

            <warning>
                <para>Слишком низкое значение может снизить производительность системы.</para>
            </warning>
        </section>

        <section xml:id="config-param-MaxUnflushedWrites">
            <title>MaxUnflushedWrites</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>MaxUnflushedWrites</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>MaxUnflushedWrites</parameter> определяет, как часто страницы
                из кэш памяти будут выгружаться на жесткий диск (активен только при значении
                параметра ForcedWrites=Off).</para>
            <para>Значение параметра <parameter>MaxUnflushedWrites</parameter> определяет
                максимальное количество накопленных страниц не сброшенных на диск, ожидающих сброса
                при подтверждения транзакции.</para>

            <para>Параметр имеет целочисленный тип и измеряется в страницах. Значение по умолчанию
                равно 100 страниц. Для не Win32 систем значение по умолчанию является -1(Отключено).
                Максимально допустимое значение равно 2147483647.</para>

            <example>
                <title>Установка параметра MaxUnflushedWrites</title>

                <para>
                    <programlisting>
MaxUnflushedWrites = 100  
                    </programlisting>
                </para>
            </example>

            <warning>
                <para>Чем больше значение параметра, тем выше вероятность потери данных при
                    возникновении аппаратного сбоя в системе.</para>
            </warning>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-MaxUnflushedWriteTime"
                        >MaxUnflushedWriteTime</link></para>
            </formalpara>
        </section>

        <section xml:id="config-param-MaxUnflushedWriteTime">
            <title>MaxUnflushedWriteTime</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>MaxUnflushedWriteTime</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-database.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>MaxUnflushedWriteTime</parameter> определяет, как часто
                страницы из кэш памяти будут выгружаться на жесткий диск (активен только при
                значении параметра ForcedWrites=Off).</para>


            <para>Значение параметра <parameter>MaxUnflushedWriteTime</parameter> определяет время,
                по истечении которого страницы данных, ожидающие сброса на диск при подтверждении
                транзакции, будут выгружены на диск.</para>

            <para>Параметр имеет целочисленный тип и измеряется в секундах. Значение по умолчанию
                равно 5 секунд. Для не Win32 систем значение по умолчанию является -1(Отключено).
                Максимально допустимое значение равно 2147483647.</para>

            <example>
                <title>Установка параметра MaxUnflushedWriteTime</title>

                <para>
                    <programlisting>
MaxUnflushedWriteTime = 5  
                    </programlisting>
                </para>
            </example>

            <warning>
                <para>Чем больше значение параметра, тем выше вероятность потери данных при
                    возникновении аппаратного сбоя в системе.</para>
            </warning>

            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-MaxUnflushedWrites"
                    >MaxUnflushedWrites</link></para>
            </formalpara>
        </section>

        <section xml:id="config-param-BugcheckAbort">
            <title>BugcheckAbort</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>BugcheckAbort</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-server.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Boolean.</para>
            </formalpara>

            <para>Опция <parameter>BugcheckAbort</parameter> определяет, прерывать ли работу сервера
                при возникновении внутренней ошибки или снимать дамп ядра для последующего анализа.
                Если опция отключена, то ядро пытается минимизировать ущерб и продолжить
                работу.</para>
            <para>Параметр имеет логический тип. Возможные значения 0 и 1. Значение по умолчанию
                равно 0, в этом случае механизм снятия дампов отключен. Для отладочных сборок
                (DEV_BUILD) значение по умолчанию равно 1.</para>

            <example>
                <title>Установка параметра BugcheckAbort</title>

                <para>
                    <programlisting>
BugcheckAbort = 0  
                    </programlisting>
                </para>
            </example>

            <note>
                <para>Обратите внимание, что установка этой опции в 1 заставляет движок производить
                    трассируемые дампы, когда внутри UDF происходит что-то неприятное, например
                    SIGSEGV. В Windows включение этой опции заставляет ядро вызывать средства
                    отладки JIT при возникновении ошибок.</para>
                <para>В POSIX при <code>BugCheckBort = 1</code> сервер Firebird будет устанавливать
                    soft-лимит для размера файлов равным hard-лимиту, и делать запись в каталог,
                    доступный для записи, как правило это <filename>/tmp</filename>.</para>
            </note>
        </section>
        <section xml:id="config-param-RelaxedAliasChecking">
            <title>RelaxedAliasChecking</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RelaxedAliasChecking</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>
            <para>Параметр <parameter>RelaxedAliasChecking</parameter> позволяет снять ограничение
                на обязательное использование псевдонимов имен таблиц в запросах. Если опция
                включена, то Firebird позволяет выполнять подобные запросы:
                <programlisting language="sql">
SELECT MY_TABLE.X FROM MY_TABLE A
            </programlisting>
            </para>
            <para>Параметр имеет логический тип. Значение по умолчанию равно 0. Если значение
                параметра равно 1, то ограничение на обязательное использование псевдонимов таблиц в
                запросах снимается.</para>
            <example>
                <title>Установка параметра RelaxedAliasChecking</title>
                <para>
                    <programlisting>
RelaxedAliasChecking = 0  
                    </programlisting>
                </para>
            </example>
            <para>Не рекомендуется включать этот параметр. Его следует рассматривать как временный
                обходной путь для портирования неаккуратного устаревшего кода до тех пор, пока не
                удастся пересмотреть такой код.</para>
            <caution>
                <para>Нет никаких гарантий что данная установка будет доступна в следующих версиях
                    Firebird.</para>
            </caution>
            <example>
                <title>Установка параметра RelaxedAliasChecking</title>
                <para>
                    <programlisting>
RelaxedAliasChecking = 0  
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-ConnectionTimeout">
            <title>ConnectionTimeout</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>ConnectionTimeout</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-connection.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>Integer.</para>
            </formalpara>

            <para>С помощью параметра <parameter>ConnectionTimeout</parameter> устанавливается
                ограничение на время ожидания соединения. После того как порог, установленный
                значением параметра, будет превышен, попытка соединения будет признана
                неудачной.</para>
            <para>Параметр <parameter>ConnectionTimeout</parameter> имеет целочисленный тип и
                измеряется в секундах. Значение по умолчанию равно 180 секунд. Минимальное значение
                равно 0. Максимально допустимое значение равно 2147483647.</para>

            <example>
                <title>Установка параметра ConnectionTimeout</title>

                <para>
                    <programlisting>
ConnectionTimeout = 180  
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-WireCrypt">
            <title>WireCrypt</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>WireCrypt</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>

                <para>Per-connection.</para>
            </formalpara>

            <formalpara>
                <title>Тип параметра:</title>

                <para>String (предопределённые значения).</para>
            </formalpara>

            <para>Параметр устанавливает, следует ли шифровать сетевое соединение. Он может
                принимать три возможных значения: <literal>Required</literal>,
                    <literal>Enabled</literal>, <literal>Disabled</literal>. По-умолчанию
                установлено, что шифрование является обязательным (<literal>Required</literal>) для
                подключений, поступающих на сервер и включенным (<literal>Enabled</literal>) для
                подключений, исходящих с клиента.</para>

            <example>
                <title>Установка параметра WireCrypt</title>

                <para>
                    <programlisting>
WireCrypt = Enabled # для клиента  
                    </programlisting>
                    <programlisting>
WireCrypt = Required # для сервера 
                    </programlisting>
                </para>
            </example>

            <para>Чтобы получить доступ к серверу с использованием традиционной
                    (<literal>Legacy_Auth</literal>) аутентификации, параметр
                    <parameter>WireCrypt</parameter> в файле конфигурации сервера должен быть
                включен (<literal>Enabled</literal>) или выключен
                (<literal>Disabled</literal>).</para>
            <para>Правила очень просты: если на одной стороне стоит значение <code>WireCrypt =
                    Required</code>, а на другой установлено значение <literal>Disabled</literal>,
                то первая сторона отклоняет соединение и оно не устанавливается. Если на одной
                стороне стоит значение <code>WireCrypt = Enabled</code>, то на другой шифрования
                может и не быть вовсе.</para>
            <para>Отсутствующий подключаемый модуль <parameter>WireCryptPlugin</parameter> или ключ
                шифрования в случаях, когда канал должен быть зашифрован, также препятствует
                соединению.</para>
            <para>Во всех остальных случаях соединение устанавливается без шифрования, если хотя бы
                одна сторона имеет <code>WireCrypt = Disabled</code>. В других случаях
                устанавливается шифрованное соединение. <table frame="all">
                    <?dbfo keep-together='auto'?>
                    <title>Совместимость параметров WireCrypt на клиенте и на сервере</title>
                    <tgroup cols="4">
                        <colspec colname="c1" colnum="1" colwidth="1.0*"/>
                        <colspec colname="c2" colnum="2" colwidth="1.0*"/>
                        <colspec colname="c3" colnum="3" colwidth="1.0*"/>
                        <colspec colname="c4" colnum="4" colwidth="1.0*"/>
                        <tbody>
                            <row>
                                <entry/>
                                <entry>Disabled</entry>
                                <entry>Enabled</entry>
                                <entry>Required</entry>
                            </row>
                            <row>
                                <entry>Disabled</entry>
                                <entry>Шифрование отключено</entry>
                                <entry>Шифрование отключено</entry>
                                <entry>Ошибка соединения</entry>
                            </row>
                            <row>
                                <entry>Enabled</entry>
                                <entry>Шифрование отключено</entry>
                                <entry>Шифрование включено, если плагин аутентификации предоставляет
                                    ключ шифрования. Иначе шифрования нет.</entry>
                                <entry>Шифрование включено, если плагин аутентификации предоставляет
                                    ключ шифрования. Иначе ошибка подключения.</entry>
                            </row>
                            <row>
                                <entry>Required</entry>
                                <entry>Ошибка соединения</entry>
                                <entry>Шифрование включено, если плагин аутентификации предоставляет
                                    ключ шифрования. Иначе ошибка подключения.</entry>
                                <entry>Шифрование включено, если плагин аутентификации предоставляет
                                    ключ шифрования. Иначе ошибка подключения.</entry>
                            </row>
                        </tbody>
                    </tgroup>
                </table></para>
            <formalpara>
                <title>Смотри также:</title>

                <para><link linkend="config-param-Auth">AuthServer и AuthClient</link>, <link
                        linkend="config-param-WireCryptPlugin">WireCryptPlugin</link>.</para>
            </formalpara>
        </section>

        <section xml:id="config-param-WireCompression">
            <title>WireCompression</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>WireCompression</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>

            <para>Параметр может быть задействован и в <filename>firebird.conf</filename> и в
                    <filename>databases.conf</filename>; он включает или отключает сжатие данных,
                передающихся по сети.</para>
            <para>По-умолчанию сжатие отключено.</para>

            <example>
                <title>Установка параметра WireCompression</title>

                <para>
                    <programlisting>
WireCompression = false  
                    </programlisting>
                </para>
            </example>

            <para>Для правильной работы параметра требуется корректная настройка как на сервере, так
                и на клиенте:<itemizedlist>
                    <listitem>
                        <para>Чтобы включить <parameter>WireCompression</parameter> на стороне
                            сервера, поставьте параметр в значение <literal>true</literal> в файле
                                <filename>firebird.conf</filename> или
                                <filename>database.conf</filename>.</para>
                    </listitem>
                    <listitem>
                        <para>Для того, чтобы активировать <parameter>WireCompression</parameter> на
                            стороне клиента, передайте соответствующий тег в вызов DPB и SPB:
                            <programlisting>
isc_dbp_config <replaceable>&lt;string-length&gt;</replaceable> "WireCompression=true"
isc_sbp_config <replaceable>&lt;string-length&gt;</replaceable> "WireCompression=true" 
                        </programlisting>
                        </para>
                    </listitem>
                    <listitem>
                        <para>Клиент и сервер должны использовать протокол >= 13 (Firebird 3.0 и
                            старше).</para>
                    </listitem>
                </itemizedlist></para>
        </section>

        <section xml:id="config-param-DummyPacketInterval">
            <title>DummyPacketInterval</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>DummyPacketInterval</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>
            <para> Параметр <parameter>DummyPacketInterval</parameter> используется для того, чтобы
                установить число секунд ожидания в <quote>тихом</quote> режиме, прежде чем сервер
                начнет посылать пустые пакеты для подтверждения соединения.</para>
            <para> Параметр имеет целочисленный тип и измеряется в секундах. Значение по умолчанию
                равно 0 секунд. Максимально допустимое значение равно 2147483647 секунд. </para>
            <warning>
                <para>Эта опция может привести к зависанию или сбоям Windows NT4 или Windows 2000
                    pre SP3 на стороне клиента, как описано в <link
                        xmlns:xlink="http://www.w3.org/1999/xlink"
                        xlink:href="https://support.microsoft.com/ru-ru/help/296265/windows-2000-non-paged-pool-is-exhausted-by-afd-sys"
                        >296265</link>, или не может предотвратить возможное отключение неактивного
                    клиента для других ОС.</para>
            </warning>

            <para>Обычно Firebird использует опцию сокета SO_KEEPALIVE, чтобы следить за активными
                подключениями по TCP/IP протоколу. Если вас не устраивает заданное по умолчанию
                2-часовое время ожидания (keepalive), то следует изменить параметры настройки своей
                операционной системы соответственно:<itemizedlist>
                    <listitem>
                        <para>в Unix-подобных ОС необходимо изменить содержимое
                                <filename>/proc/sys/net/ipv4/tcp_keepalive_*</filename>;</para>
                    </listitem>
                    <listitem>
                        <para>в Windows необходимо вносить изменения в ветку реестра
                                <filename>HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters\</filename>
                            ключи (KeepAliveTime, KeepAliveInterva,
                            TcpMaxDataRetransmissions).</para>
                    </listitem>
                </itemizedlist>
            </para>
        </section>

        <section xml:id="config-param-RemoteServicePort">
            <title>RemoteServicePort или RemoteServiceName</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteServicePort</secondary>
            </indexterm>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteServiceName</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer или String.</para>
            </formalpara>
            <para>Параметры <parameter>RemoteServiceName</parameter> и
                    <parameter>RemoteServicePort</parameter> используются для установки номера порта
                или имени сервиса, которые будут использоваться для клиентских соединений с базами
                данных.</para>
            <para>Параметр <parameter>RemoteServiceName</parameter> имеет строковый тип. Значение по
                умолчанию равно <literal>gds_db</literal>.</para>
            <para>Параметр <parameter>RemoteServicePort</parameter> имеет целочисленный тип.
                Значение по умолчанию равно <literal>3050</literal>.</para>
            <example>
                <title>Установка параметра RemoteServiceName</title>

                <para>
                    <programlisting>
RemoteServiceName = gds_db 
                    </programlisting>
                </para>
            </example>
            <example>
                <title>Установка параметра RemoteServicePort</title>

                <para>
                    <programlisting>
RemoteServicePort = 3050 
                    </programlisting>
                </para>
            </example>
            <important>
                <para>Изменять следует только один из этих параметров, не оба сразу. Сервер ищет
                    номер порта для клиентских соединений в следующем порядке — сначала
                        <parameter>RemoteServiceName</parameter> (соответствующая значению параметра
                    запись ищется в файле <filename>services</filename>), затем
                        <parameter>RemoteServicePort</parameter>.</para>
            </important>

            <note>
                <para>Обычно один из этих параметров (чаще всего
                        <parameter>RemoteServicePort</parameter>) меняют только на стороне севера в
                        <filename>firebird.conf</filename>, поскольку на клиентской стороне имя
                    сервиса или номер порта можно указать в строке подключения.</para>
            </note>
        </section>
        <section xml:id="config-param-RemoteAuxPort">
            <title>RemoteAuxPort</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteAuxPort</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>
            <para>Параметр <parameter>RemoteAuxPort</parameter> определяет номер TCP-порта, который
                будет использоваться для передачи уведомлений о событиях сервера.</para>
            <para>Параметр <parameter>RemoteAuxPort</parameter> имеет целочисленный тип. Значение по
                умолчанию равно 0. В этом случае номер порта будет выбираться случайно.</para>
            <example>
                <title>Установка параметра RemoteAuxPort</title>

                <para>
                    <programlisting>
RemoteAuxPort = 0 
                    </programlisting>
                </para>
            </example>

        </section>
        <section xml:id="config-param-TcpRemoteBufferSize">
            <title>TcpRemoteBufferSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TcpRemoteBufferSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>
            <para>Параметр <parameter>TcpRemoteBufferSize</parameter> определяет размер TCP/IP
                буфера для обмена сообщениями между сервером и клиентом. Firebird может делать
                упреждающее чтение клиентом и может отправлять несколько строк за один сетевой
                пакет. Чем больше размер пакета, тем больше данных будет передаваться за одну
                передачу.</para>
            <para>Параметр имеет целочисленный тип и измеряется в байтах. Значение по умолчанию
                равно 8192. Минимально допустимое значение равно 1448. Максимальное значение равно
                32767.</para>
            <example>
                <title>Установка параметра TcpRemoteBufferSize</title>
                <para>
                    <programlisting>
TcpRemoteBufferSize = 8192 
                    </programlisting>
                </para>
            </example>
        </section>
        <section xml:id="config-param-TcpNoNagle">
            <title>TcpNoNagle</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TcpNoNagle</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>
            <para>Параметр <parameter>TcpNoNagle</parameter> включает или отключает использование
                Nagle алгоритма (опция TCP_NODELAY для сокета) в TCP/IP соединениях.</para>
            <para>В Linux по умолчанию библиотека сокетов минимизирует количество физических записей
                путем буферизации записей перед фактической передачей данных. Для этого используется
                встроенный алгоритм, известный как Nagle's Algorithm. Он был разработан, для того,
                чтобы избежать проблем с маленькими пакетами в медленных сетях.</para>
            <para>Параметр имеет логический тип. По умолчанию значение параметра равно 1 (истина). В
                этом случае буферизация не используется. На медленных сетях в Linux это позволяет
                увеличить скорость передачи.</para>
            <example>
                <title>Установка параметра TcpNoNagle</title>
                <para>
                    <programlisting>
TcpNoNagle = 1 
                    </programlisting>
                </para>
            </example>
        </section>
        <section xml:id="config-param-TcpLoopbackFastPath">
            <title>TcpLoopbackFastPath</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>TcpLoopbackFastPath</secondary>
            </indexterm>

            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>

            <para>Параметр <parameter>TcpLoopbackFastPath</parameter> включает или отключает
                использование функции <quote>TCP Loopback Fast Path</quote>
                (SIO_LOOPBACK_FAST_PATH).</para>
            <para>Применим только в Windows (версия 8/2012 и выше). Параметр доступен начиная с
                Firebird 3.0.5.</para>
            <para>Параметр имеет логический тип. По умолчанию значение параметра равно 1
                (истина).</para>
            <example>
                <title>Установка параметра TcpLoopbackFastPath</title>
                <para>
                    <programlisting>
TcpLoopbackFastPath = 1 
                    </programlisting>
                </para>
            </example>
        </section>
        <section xml:id="config-param-IPv6V6Only">
            <title>IPv6V6Only</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>IPv6V6Only</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>

            <para>Этот параметр можно устанавливать только в <filename>firebird.conf</filename>.
                Firebird поддерживает <emphasis role="bold">IPv6</emphasis> подключение на стороне
                сервера и клиента. Параметр может принимать значения
                    <literal>true</literal>/<literal>false</literal>,
                    <literal>1</literal>/<literal>0</literal> или
                    <literal>Yes</literal>/<literal>No</literal>. Значение по умолчанию равно
                    <literal>false</literal>.</para>

            <note>
                <para>В Windows эта опция поддерживается, начиная с Windows Vista, в более ранних
                    версиях значение параметра всегда равно <literal>true</literal>.</para>
            </note>

            <example>
                <title>Установка параметра IPv6V6Only</title>
                <para>
                    <programlisting>
IPv6V6Only = 0 
                    </programlisting>
                </para>
            </example>

            <simplesect>
                <title>Сервер</title>

                <para>По-умолчанию, сервер прослушивает пустой <emphasis role="bold">IPv6</emphasis>
                    адрес (<literal>::</literal>) и принимает все входящие подключения, будь то
                        <emphasis role="bold">IPv4</emphasis> или <emphasis role="bold"
                        >IPv6</emphasis> (<code>IPv6V6Only = false</code>). Если параметр установлен
                    в <literal>true</literal>, сервер, прослушивая явно или неявно пустой IPv6
                    адрес, принимает только IPv6 подключения.</para>

            </simplesect>
            <simplesect>
                <title>Клиент</title>

                <para>Адреса <emphasis role="bold">IPv6</emphasis> отображаются как восемь
                    четырёхзначных шестнадцатеричных чисел (то есть групп по четыре символа),
                    разделённых двоеточием. В строке подключения необходимо заключать <emphasis
                        role="bold">IPv6</emphasis> адрес в квадратные скобки, чтобы разрешить
                    неоднозначность с использованием двоеточия в качестве разделителя между IP
                    адресом хоста и путем к базе данных. К примеру:
                    <programlisting>
connect '[2014:1234::5]:test';
connect '[2014:1234::5]/3049:/srv/firebird/test.fdb';                
                </programlisting>
                </para>
            </simplesect>
        </section>

        <section xml:id="config-param-RemoteBindAddress">
            <title>RemoteBindAddress</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemoteBindAddress</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String.</para>
            </formalpara>
            <para>Параметр <parameter>RemoteBindAddress</parameter> позволяет привязать входящие
                соединения к IP адресу определенной сетевой карты. При этом все входящие соединения
                через другие сетевые интерфейсы будут запрещены. По умолчанию подключения из любого
                доступного сетевого интерфейса разрешены. Если вы используете классический сервер,
                этот параметр предназначен только для Windows. В Linux, BSD или Mac OS X с сервером
                Classic используйте конфигурационный файл xinetd или launchd (параметр
                    <parameter>bind</parameter>).</para>
            <para>Параметр имеет строковый тип. По умолчанию его значение равно пустой строке
                (разрешены соединения с любого IP адреса).</para>
            <example>
                <title>Установка параметра RemoteBindAddress</title>
                <para>
                    <programlisting>
RemoteBindAddress =  
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-LockMemSize">
            <title>LockMemSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>LockMemSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>Значение параметра <parameter>LockMemSize</parameter> определяет объем памяти,
                которая будет выделена менеджеру блокировок. В архитектуре Classic и SuperClassic
                данный параметр используется для начального распределения, далее таблица расширяется
                динамически до предела памяти. В архитектуре Super значение параметра определяет
                начальное распределение и предел выделяемой памяти.</para>
            <para>Параметр имеет целочисленный тип. Единица измерения — байты. Значение по умолчанию
                равно 1 Мбайт. Минимальное значение равно 0. максимальное — 2 Гбайта.</para>

            <example>
                <title>Установка параметра LockMemSize</title>
                <para>
                    <programlisting>
LockMemSize = 1M 
                    </programlisting>
                </para>
            </example>

            <para>На размер таблицы блокировок влияют: <orderedlist>
                    <listitem>
                        <para>В архитектурах Classic и SuperClassic размер страничного кэша (Super
                            сервер для блокировки страниц использует лёгковесные защёлки). Страница,
                            помещенная в кэш, блокируется, как минимум, один раз, страницы, которые
                            читаются несколькими клиентами, могут блокироваться несколько
                            раз.</para>
                    </listitem>
                    <listitem>
                        <para>Число одновременных транзакций. Каждая транзакция имеет блокировку.
                            Блокировка используется для синхронизации транзакций.</para>
                    </listitem>
                    <listitem>
                        <para>События. Механизм оповещения о событиях использует блокировки. Число
                            событий и число клиентов, ожидающих эти события, влияют на размер
                            таблицы блокировок.</para>
                    </listitem>
                </orderedlist></para>
        </section>

        <section xml:id="config-param-LockAcquireSpins">
            <title>LockAcquireSpins</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>LockAcquireSpins</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>В архитектуре сервера Classic только одно клиентское соединение может обратиться к
                таблице блокировки в одно и то же время. Доступ к таблице блокировки управляется с
                помощью mutex(а). Mutex может быть затребован в условном, либо безусловном режиме.
                Если mutex затребован в условном режиме, то ожидание является отказом, и запрос
                должен повториться. В безусловном режиме mutex будет ожидаться до тех пор, пока не
                будет получен.</para>
            <para>Параметр <parameter>LockAcquireSpins</parameter> имеет целочисленный тип. Его
                значение устанавливает количество попыток, которые будут сделаны в условном режиме.
                По умолчанию значение параметра равно 0, в этом случае будет использоваться
                безусловный режим.</para>

            <note>
                <para> Параметр имеет эффект только на SMP (симметричных мультипроцессорных)
                    системах. </para>
            </note>

            <example>
                <title>Установка параметра LockAcquireSpins</title>
                <para>
                    <programlisting>
LockAcquireSpins = 2 
                    </programlisting>
                </para>
            </example>

            <note>
                <para>В залоговке лок-таблицы этот параметр отображается как <quote>Spin
                        count</quote> в строке вида:</para>
                <screen>
Acquires:   2150, Acquire blocks:     14, Spin count:   0                    
                </screen>
            </note>
        </section>

        <section xml:id="config-param-LockHashSlots">
            <title>LockHashSlots</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>LockHashSlots</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>LockHashSlots</parameter> используется для настройки числа
                слотов хэш таблицы блокировок. Чем больше слотов используется, тем короче получаются
                хэш цепочки, что увеличивает производительность при повышенной нагрузке.</para>
            <para>Параметр имеет целочисленный тип. Максимально допустимое значение этого параметра
                равно 65521 (значения больше этого порога игнорируются и используется всё равно
                именно этот порог: 65521). </para>
            <para>Значение по умолчанию (8191) можно считать подходящим для работы 100 пользователей
                и умалчиваемом размере страничного кеша (256).</para>
            <para>В качестве значения рекомендуется указывать простое число, чтобы хэш-алгоритм
                производил хорошее распределение. </para>

            <example>
                <title>Установка параметра LockHashSlots</title>
                <para>
                    <programlisting>
LockHashSlots = 30011 
                    </programlisting>
                </para>
            </example>

            <para>Увеличение значения данного параметра необходимо только при высокой загрузке
                (одновременно с ним следует увеличить и параметр <parameter>LockMemSize</parameter>
                на тот же процент). Он вычисляется с использованием утилиты Lock Print по следующему
                принципу.</para>

            <para>Запускаем утилиту
                <programlisting>
fb_lock_print -d <replaceable>&lt;database&gt;</replaceable> | <replaceable>&lt;alias&gt;</replaceable>            
            </programlisting>
            </para>

            <para>В группе заголовка блока (<emphasis role="bold">LOCK_HEADER BLOCK</emphasis>),
                которая описывает основную конфигурацию и состояние таблицы блокировок, смотрим
                значение элемента <emphasis role="bold">Hash lengths</emphasis> (длина хэш цепочки).
                Этот элемент сообщает минимальную, среднюю и максимальную длину цепочки слотов. Чем
                длиннее будут цепочки, тем медленнее будет работать менеджер блокировок. Если
                среднее значение больше 3 или максимальное больше 10, то это означает, что слотов
                недостаточно. Поэтому следует увеличить параметр
                    <parameter>LockHashSlots</parameter> в 2-3 раза (при этом взять простое
                число).</para>

            <note>
                <para>Для применения параметра необходимо, чтобы сервер пересоздал таблицу
                    блокировок (при этом в системе не должно остаться подключений и старой таблицы
                    блокировок).</para>
            </note>
        </section>
        <section xml:id="config-param-EventMemSize">
            <title>EventMemSize</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>EventMemSize</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>Значение параметра <parameter>EventMemSize</parameter> определяет объем
                разделяемой памяти, которая будет выделена менеджеру событий.</para>
            <para>Параметр <parameter>EventMemSize</parameter> имеет целочисленный тип. Единица
                измерения — байты. Значение по умолчанию равно 64 Кбайта. Минимально допустимое
                значение равно 0. Максимальное значение равно 2 Гбайта.</para>
            <example>
                <title>Установка параметра EventMemSize</title>
                <para>
                    <programlisting>
EventMemSize = 64K
                    </programlisting>
                </para>
            </example>
        </section>
    </section>

    <section xml:id="config-engine">
        <title>Настройки ядра</title>

        <section xml:id="config-param-CpuAffinityMask">
            <title>CpuAffinityMask</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>CpuAffinityMask</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>Параметр <parameter>CpuAffinityMask</parameter> позволяет указать, какие
                процессоры (ядра) будут использоваться сервером (только для ОС Windows в архитектуре
                Super сервер).</para>
            <note>
                <para>Параметр имеет эффект только в SMP (симметричных мультипроцессорных)
                    системах.</para>
            </note>

            <para>Параметр имеет целочисленный тип. Значение параметра соответствует элементам
                битового массива, в котором каждый бит представляет центральный процессор. Таким
                образом, чтобы использовать только первый процессор, значение параметра должно быть
                равно 1. Чтобы использовать и центральный процессор 1, и центральный процессор 2 —
                3. Чтобы использовать центральный процессор 2, и центральный процессор 3 — 6.
                Значение по умолчанию равно 0 (могут быть использованы все доступные
                процессоры).</para>

            <example>
                <title>Установка параметра CpuAffinityMask</title>
                <para>
                    <programlisting>
CpuAffinityMask = 64K
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-GCPolicy">
            <title>GCPolicy</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>GCPolicy</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>GCPolicy</parameter> используется для управления работой
                    <quote>сборщика мусора</quote>. Параметр имеет строковый тип. Возможные значения параметра:<itemizedlist>
                    <listitem>
                        <para><literal>background</literal> — сборщик мусора работает как фоновый,
                            собирая мусор в отдельном потоке;</para>
                    </listitem>
                    <listitem>
                        <para><literal>cooperative</literal> — сборщик мусора работает в оперативном
                            режиме, собирая мусор немедлен- но при чтении <quote>мусорных</quote>
                            версий;</para>
                    </listitem>
                    <listitem>
                        <para><literal>combined</literal> — сборщик мусора работает в оперативном
                            режиме, но если мусор собрать не удается, то о
                                <quote>замусоренных</quote> страницах сигнализируется фоновому
                            сборщику мусора.</para>
                    </listitem>
                </itemizedlist></para>

            <para>По умолчанию в архитектуре Super сервера <quote>сборщик мусора</quote> работает в
                комбинированном режиме. В архитектурах Classic и SuperClassic этот параметр
                игнорируется, а <quote>сборщик мусора</quote> всегда работает в оперативном
                режиме.</para>

            <example>
                <title>Установка параметра GCPolicy</title>
                <para>
                    <programlisting>
GCPolicy = combined
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-SecurityDatabase">
            <title>SecurityDatabase</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>SecurityDatabase</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-database.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String (путь к базе данных или алиас).</para>
            </formalpara>

            <para>Определяет имя и расположение базы данных безопасности, в которой хранятся имена
                пользователей и пароли, используемые сервером для проверки удаленных
                подключений.</para>
            <para>По-умолчанию в <filename>firebird.conf</filename>:
                <programlisting>
SecurityDatabase = $(dir_secDb)/security3.fdb            
            </programlisting>
            </para>
            <para>Параметр может быть переопределен для определенной базы данных в файле
                    <filename>databases.conf</filename>. Любая база данных может быть базой данных
                безопасности, в том числе и для самой себя.</para>

        </section>
    </section>

    <section xml:id="config-windows">
        <title>Настройки для Windows систем</title>

        <section xml:id="config-param-GuardianOption">
            <title>GuardianOption</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>GuardianOption</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>

            <para>Параметр определяет должен ли сторож (Guardian) запускать сервер после того, как
                его работа была завершена некорректно.<itemizedlist>
                    <listitem>
                        <para>0 — сервер стартует единожды;</para>
                    </listitem>
                    <listitem>
                        <para>1 — сервер стартует каждый раз после некорректного завершения.</para>
                    </listitem>
                </itemizedlist></para>

            <para>Параметр имеет логический тип. Значение по умолчанию равно 1 (истина).</para>

            <example>
                <title>Установка параметра GuardianOption</title>
                <para>
                    <programlisting>
GuardianOption = 1
                    </programlisting>
                </para>
            </example>

            <note>
                <para>В современных версиях Windows службы могут перезапускаться при аварийном
                    завершении, если указаны соответствующие настройки. Это делает сторож (Guardian)
                    не нужным при установке Firebird в качестве службы. Однако он всё ещё может быть
                    полезным, если Firebird стартует в режиме приложения.</para>
            </note>
        </section>

        <section xml:id="config-param-ProcessPriorityLevel">
            <title>ProcessPriorityLevel</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>ProcessPriorityLevel</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Integer.</para>
            </formalpara>

            <para>Параметр определяет уровень приоритетов процессов сервера Firebird. Параметр имеет
                целочисленный тип и может принимать значения:<itemizedlist>
                    <listitem>
                        <para>0 — нормальный приоритет (значение по умолчанию);</para>
                    </listitem>
                    <listitem>
                        <para>положительное значение — повышенный приоритет (тоже самое что опция -B
                            в командной строке);</para>
                    </listitem>
                    <listitem>
                        <para>отрицательное значение — пониженный приоритет.</para>
                    </listitem>
                </itemizedlist></para>

            <important>
                <para>Все изменения данного параметра должны быть тщательно проверены, чтобы
                    гарантировать, что сервер продолжает обрабатывать запросы.</para>
            </important>

            <example>
                <title>Установка параметра ProcessPriorityLevel</title>
                <para>
                    <programlisting>
ProcessPriorityLevel = 0
                    </programlisting>
                </para>
            </example>
        </section>

        <section xml:id="config-param-IpcName">
            <title>IpcName</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>IpcName</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>IpcName</parameter> определяет имя области разделяемой памяти
                используемой в качестве транспортного канала в локальном протоколе. Параметр имеет
                строковый тип. Значение по умолчанию равно <literal>FIREBIRD</literal>.</para>

            <note>
                <para>Локальный протокол не совместим с Firebird версия которого меньше 2.0.</para>
            </note>

            <example>
                <title>Установка параметра IpcName</title>
                <para>
                    <programlisting>
IpcName = FIREBIRD
                    </programlisting>
                </para>
            </example>

            <note>
                <itemizedlist>
                    <listitem>
                        <para>Сервер может регистрировать объекты в пространстве имен Global, только
                            если он выполняется под учетной записью с привилегией
                            SE_CREATE_GLOBAL_NAME. Это означает, что, если вы работаете под
                            ограниченной учетной записью в Vista, XP SP2 или 2000 SP4, возможность
                            использования локального протокола для других сеансов будет
                            недоступна.</para>
                    </listitem>
                    <listitem>
                        <para>Если на одном сервере установлено несколько экземпляров Firebird, и
                            все они содержат одно и то же значение параметра IpcName (явно заданное
                            или умалчиваемое), то firebird.log всех запускаемых экземпляров, начиная
                            со второго, будет при каждом (ре-)старте пополняться сообщением
                            вида:</para>
                        <screen>
XNET error: XNET server initialization failed. Probably another instance 
of server is already running. 
operating system directive CreateMutex failed
Невозможно создать файл, так как он уже существует.                            
                        </screen>
                        <para>Для каждого подключения к СУБД может быть назначено индивидуальное
                            значение этого параметра.</para>
                    </listitem>
                </itemizedlist>
            </note>
        </section>

        <section xml:id="config-param-RemotePipeName">
            <title>RemotePipeName</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>RemotePipeName</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-connection.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>RemotePipeName</parameter> определяет название канала (Pipe),
                используемого как транспортный канал в протоколе NetBEUI. Название канала в
                протоколе NetBEUI имеет то же самое значение, что и номер порта для протокола
                TCP/IP.</para>
            <para>Параметр имеет строковый тип. Значение по умолчанию равно
                    <literal>interbas</literal> и совместимо с InterBase/Firebird 1.0.</para>

            <example>
                <title>Установка параметра RemotePipeName</title>
                <para>
                    <programlisting>
RemotePipeName = interbas
                    </programlisting>
                </para>
            </example>
        </section>
    </section>

    <section xml:id="config-unix">
        <title>Настройки для Unix/Linux систем</title>

        <section xml:id="config-param-Redirection">
            <title>Redirection</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>Redirection</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>Boolean.</para>
            </formalpara>

            <para>Параметр <parameter>Redirection</parameter> используется для отключения защиты от
                переадресации запросов на другие сервера. Возможность переадресации запросов на
                другие серверы изначально присутствовала в InterBase. Но она была исключена
                корпорацией Borland в InterBase 6.0 после доработки добавившей SQL-диалекты.
                Возможность перенаправления запросов была восстановлена в Firebird 2.0.</para>

            <warning>
                <para>На сегодняшний день использование этой возможности (прокси сервер)
                    представляет угрозу безопасности. Например, вы используете защищенный сервер
                    Firebird, доступ к которому осуществляется из глобальной сети. В этом случае,
                    если у сервера есть доступ к локальной сети, то он будет исполнять роль шлюза
                    для входящих запросов типа:
                    <programlisting>
firebird.your.domain.com:internal_server:/private/database.fdb
            </programlisting>
                </para>
                <para>При этом злоумышленнику достаточно знать имя или IP-адрес хоста вашей
                    локальной сети, потому что для соединения не требуется знать логин и пароль на
                    внешнем сервере. Такой шлюз позволяет обойти систему сетевой защиты,
                    установленную в вашей локальной сети.</para>
            </warning>

            <para>Параметр имеет логический тип. Значение по умолчанию равно 0
                    (<literal>false</literal>). В этом случае возможность перенаправления запросов
                отключена. Для включения этой опции следует значение параметра выставить равным 1
                    (<literal>true</literal>).</para>

            <example>
                <title>Установка параметра Redirection</title>
                <para>
                    <programlisting>
Redirection = 0
                    </programlisting>
                </para>
            </example>

            <warning>
                <para>Не включайте эту опцию если вы не уверены, что именно она делает.</para>
            </warning>
        </section>
    </section>

    <section xml:id="config-servermode">
        <title>Настройки архитектуры</title>

        <section xml:id="config-param-ServerMode">
            <title>ServerMode</title>

            <indexterm>
                <primary>Параметр конфигурации</primary>
                <secondary>ServerMode</secondary>
            </indexterm>

            <formalpara>
                <title>Область действия:</title>
                <para>Per-server.</para>
            </formalpara>
            <formalpara>
                <title>Тип параметра:</title>
                <para>String.</para>
            </formalpara>

            <para>Параметр <parameter>ServerMode</parameter> определяет архитектуру сервера.
                Существует 3 варианта архитектуры: <itemizedlist>
                    <listitem>
                        <para><literal>Super</literal> (или <literal>ThreadedDedicated</literal>) —
                            база данных открывается эксклюзивно одним серверным процессом, им же
                            обслуживаются все соединения с базой данных. Подключения обрабатываются
                            потоками из общего пула; используется общий страничный кэш на каждую
                            базу данных для всех соединений и общий кэш для временного пространства
                            (сортировок).</para>
                    </listitem>
                    <listitem>
                        <para><literal>SuperClassic</literal> (или
                            <literal>ThreadedShared</literal>) — базы данных открываются одним
                            серверным процессом, но доступ не исключительный — embedded процессы
                            могут открыть одновременно одну ту же базу. Подключения обрабатываются
                            потоками из общего пула; используется собственный страничный кэш для
                            каждого соединения и общий кэш для временного пространства
                            (сортировок).</para>
                    </listitem>
                    <listitem>
                        <para><literal>Classic</literal> (или <literal>MultiProcess</literal>) —
                            создаётся отдельный процесс на каждое соединение с базой данных. Каждая
                            база данных может быть открыта несколькими процессами (включая локальные
                            для embedded доступа); используется отдельный кеш страниц на каждое
                            соединение и отдельный кэш для временного пространства
                            (сортировок).</para>
                    </listitem>
                </itemizedlist></para>
        </section>
    </section>
</chapter>
